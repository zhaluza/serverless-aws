/**
 * Use mode: 'production' to minify code.
 * Using 'none' gives you an advantage while developing,
 * since the logs will tell you where the code failed
 */

module.exports = {
  target: "node",
  mode: "none",
};
